# Migrations Folder

This project is structured to support SQLAlchemy database migrations.

When you are ready to track schema changes, initialize Alembic/Flask-Migrate and store the generated migration scripts here.
